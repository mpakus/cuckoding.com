#!/bin/sh
# Desktop control plane is loopback-only. Erlang distribution is not required
# for the local .app and must not listen on all interfaces.
export RELEASE_DISTRIBUTION="${RELEASE_DISTRIBUTION:-none}"
