# Worklog — 0603 usage, cost, and confidence accounting

## Metadata

- Date/time (UTC): 2026-09-17
- Task: 0603
- Status: complete
- Human/agent owner: codex
- Branch: `feature/0603-usage-cost`
- Start revision: `741e2dd`
- End revision: pending commit

## Acceptance criteria

- Persist adapter usage with its agent-session attribution, provider source, confidence, token dimensions, and reported monetary cost when available.
- Prefer provider-reported cost; otherwise estimate from the versioned catalog effective at usage time and persist the exact integer-micros formula inputs.
- Mark estimates with `≈`, preserve unavailable values as unavailable, and never infer a subscription-plan marginal cost.
- Store plugin optimization claims as a separate reported dimension rather than subtracting them from tokens or provider cost.
- Reconcile frozen catalog fixtures exactly within the documented integer rounding boundary.

## Reference coding

- Focused project, Vibe Kanban, Agetor, and Hydra XERJ searches were attempted first; the configured node at `127.0.0.1:9200` was unreachable, so retrieval is degraded.
- Inspected Agetor revision `eb74ab5f3d6d71dfb91d5bd34e9c679c5a955a6a` under MIT. `src/bun/fx-acp.ts:1832-1856` validates usage independently from optional monetary cost and preserves valid usage when cost is malformed.
- Inspected Hydra revision `d8ad56112c2c3acfb2f65f53b6890f30a25c693c` under MIT. `src/components/UsageDashboard/UsageDashboard.tsx:86-89,346-370` describes local-log analysis and normalizes token/cost dimensions.
- Adapted only the separation of usage and cost facts. Cuckoding adds immutable effective-dated catalogs, integer formulas, explicit billing mode, session-scoped replay protection, separate cost provenance, and independent append-only plugin claims; no peer code was copied.

## Work performed

- Claimed task 0603 and restated its acceptance criteria.
- Added immutable price-catalog versions with validated ISO currency and non-negative integer micros-per-million rates.
- Added append-only, event-idempotent usage facts attributed to an agent session, provider, model, and usage timestamp without storing raw provider event keys.
- Added provider-cost precedence and catalog estimates only for explicitly declared API billing; subscription, unknown mode, missing catalog, and incomplete rates remain unavailable.
- Recorded the complete integer formula and one final half-up rounding step so every estimate is reproducible to the micro.
- Preserved explicitly reported reasoning-token usage from the Codex adapter.
- Added append-only plugin optimization claims with computed saved units as a separate dimension that never changes provider usage or cost.
- Redacted plugin metadata recursively before persistence and preserved usage attribution when the provider model is unknown.
- Added an accessible recent-usage table with provider, token, cost, and source columns; catalog estimates visibly use `≈`.
- Updated architecture, database, telemetry, development, and implementation-plan documentation.

## Verification

- `rtk env MIX_ENV=test mix ecto.reset` — passed against the disposable test database; every migration applied cleanly from an empty prior schema, including accounting constraints and append-only triggers.
- `rtk mix test test/cuckoding/telemetry/accounting_test.exs test/cuckoding/adapters/claude_code_test.exs test/cuckoding/adapters/codex_test.exs test/cuckoding_web/live/status_live_test.exs` — passed: 17 tests, 0 failures. Covers provider-cost precedence, event replay idempotency, effective-dated catalog selection, exact five-dimension formula reconciliation, half-up micro rounding, subscription non-estimation, missing-rate refusal, unknown-model attribution, plugin separation and metadata redaction, estimate UI labels, and adapter reasoning tokens.
- `rtk mix compile --warnings-as-errors` — passed after correcting a multi-clause default declaration.
- The first full quality pass passed all behavioral tests but Credo found four formula/provenance complexity opportunities. The branches were split into small pattern-matched helpers without changing behavior; strict Credo then passed independently.
- Final `rtk mix quality` — passed: formatter check; compiler with warnings as errors; 10 properties and 130 tests with 0 failures; Credo strict over 108 source files and 1,494 modules/functions with no issues; Sobelow clean; Hex audit found no retired or advisory packages.
- `rtk git diff --check` — passed before commit.

## Handoff

Complete. Reported and estimated costs are visibly distinct, formulas are reproducible, subscription marginal cost remains unavailable, and plugin claims cannot alter provider usage facts.
