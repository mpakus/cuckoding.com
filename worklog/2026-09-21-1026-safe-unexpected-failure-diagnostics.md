# 1026 — Safe unexpected-failure diagnostics

Claimed on `fix/1026-safe-failure-location` from clean `main`. Acceptance is the task checklist. Ponytail 4.10.0 (MIT, full), observability, workflow-and-kanban, Elixir/Phoenix LiveView, security-review, and quality-gates apply. XERJ project search was unavailable: no node answered on loopback port 9200; the current guard, executor, run view, and tests were inspected directly.

Observed read-only on the existing blocked delivery task: run `01a0bcd7-0e7d-7df7-8263-a71361f5de57` recorded a Development-stage `unexpected_failure` before an agent session was persisted. The public event has a safe code but not the exception's source location. This historical event cannot establish its root cause, and no provider will be launched to guess it. Scope: improve the next occurrence's non-secret location evidence while retaining the existing failure/state boundary and prior data.

Acceptance restated: bounded source location for unexpected application exceptions; clear run-page display; no raw exception content or private path; focused regression and quality checks; honest docs. No database migration or live-run mutation is planned.

Implementation: the shared planning/delivery failure guard derives only the first in-application module/function/line from an unexpected stack, bounded to 200 bytes. It keeps the existing `unexpected_failure` code and fixed public summary. The run alert labels this as a diagnostic location, not a root cause; older events render without it. Returned provider errors do not receive a fabricated location. No exception value, message, args, stack file path, or provider output is persisted. Tests assert the redacted canary does not appear, the location is bounded and path-free, and both new and old-style alert states render.

Verification: `rtk env -u CR_PAT mix test test/cuckoding/walking_skeleton_test.exs` passed (23 tests, 0 failures). The first `rtk env -u CR_PAT mix quality` passed 289 tests/10 properties but strict Credo found excessive nesting; the helper was simplified. The next full run passed but a subsequent test assertion needed formatting. After `rtk env -u CR_PAT mix format`, the final `rtk env -u CR_PAT mix quality` passed (289 tests, 10 properties, 0 failures; no Credo findings; Sobelow scan complete; no retired/advisory packages). `rtk git diff --check` passed. The expected plugin-supervisor fixture logs intentional child crashes; they were not test failures. No RTK proxy exception was used for verification; read-only source and XERJ inspection used `rtk proxy` and `rtk xerj` respectively.

Residual: the existing blocked Development run predates this code and still has no diagnostic site. Its exact failure cannot be inferred from a generic event; a safe, authorized provider retest after key-rotation confirmation is required before beta acceptance. This change is source evidence, not signed-app or real-provider evidence.
