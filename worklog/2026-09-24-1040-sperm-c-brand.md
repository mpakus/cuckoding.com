# 1040 — Sperm-cell C identity

## Scope

One recognizable C-shaped cell: oval head and a curved, tapered tail. Pearl
tile/violet mark for app and web, matching black alpha silhouette for macOS
template rendering. Cover marketing header/footer, app sidebar, both browser
favicons, app PNG/ICNS and tray RGBA. Retain accessible names and editable SVG.

Clean main inspected before branch `feature/1040-sperm-c-brand`. Existing icons
were a dark C/green dot app icon, text/dot web marks and a knowledge-art favicon.
Use the existing SVG and native export tools, appropriate for this vector logo
and small-size tray mark. No generated raster concept or image-generation API
is needed. Ponytail full (4.10.0, MIT), menubar-shell and quality-gates apply.
RTK proxy is used for exact SVG/source reads, export commands and bounded native
process metadata, never raw argv/environment. No new dependency is required.

## Acceptance and verification

## Artwork and integration

Updated `desktop/icon.svg` with an oval head attached to a curved tapering C,
violet shading and a pearl tile. `desktop/export_icons.rb` uses existing local
SVG/ImageMagick/iconutil tooling to export ten matching assets, including the
1024 px app icon, ICNS, 16/32/64 px ICOs and tile-free 64 px black/alpha tray data.
The exporter strips export metadata and repeated generation produced identical
SHA-256 hashes for all ten outputs. Existing raw-RGBA/template shell code stays
unchanged. The root/legacy PNG and WebP copies are updated too.

Pages header/footer and app sidebar now use the shared vector instead of a
text C/dot. Both browser surfaces advertise SVG and ICO favicons; the app no
longer uses the knowledge illustration as its favicon. Text product names
remain accessible and image alt text stays empty beside them. Pages verification
now distinguishes the three alternate illustrations from static brand images
and checks brand/fallback asset agreement across surfaces. No new runtime
dependency, workflow behavior, credentials or permission setting changed.

## Verification

- `rtk env -u GEM_HOME -u GEM_PATH /usr/bin/ruby desktop/export_icons.rb`: passed.
  The host Ruby reports an existing world-writable PATH directory warning;
  exports succeed. An initial unisolated Ruby call also reported unrelated
  missing gem-extension warnings; subsequent calls use system Ruby without
  GEM_HOME/GEM_PATH. No environment permission was changed.
- `rtk ruby github.page/verify.rb`: passed, 10 local references, 4 pinned actions,
  and matching brand/fallback assets.
- `rtk node test/github_page_art_mode_test.cjs` and
  `rtk node test/github_page_motion_test.cjs`: passed.
- `rtk env -u CR_PAT mix test test/cuckoding_web/status_live_test.exs test/cuckoding_web/agent_floor_live_test.exs`:
  16 tests, 0 failures.
- `rtk mix format --check-formatted`: passed after formatting the new HEEx image.
  `rtk mix credo --strict` and `rtk git diff --check`: passed.
- Image metadata confirms 1024 px app PNG, 16/32/64 px ICO entries, and 256 px
  compatibility WebP. Inspected the large color icon and enlarged tray alpha
  silhouette. Regenerating all ten exports preserved every byte hash.
- CUA desktop (972 px) and 320 px previews: matching header/footer marks loaded,
  SVG/ICO favicon links present, no horizontal overflow, and Classic/Irony
  switching leaves the brand unchanged. Native radio keyboard switching passed;
  browser warning/error entries were empty. Viewport reset and preview tab closed.

## Built and running evidence

Committed source/artifacts as `472a8bf` and fast-forward merged local main.
The owned preview and idle native shell/child were stopped gracefully after
PID/start/executable/cwd checks; no active native run existed. No unrelated
provider app was stopped and no migration or data reset was required.

`rtk env -u CR_PAT ./bin/dev.build` passed from clean `472a8bf`: production
assets and warnings-as-errors compilation, release assembly, 6 metadata tests
with 15 assertions, release promotion checks, 10 native tests including the
monochrome tray regression, Rustfmt/Clippy, bundle creation, and every sterile
desktop verifier check. Release assembly reported one future-mtime correction
for the edited layout; it completed successfully. Native verifier checks cover
bootstrap, authorization, replay, diagnostics, graceful shutdown, crashes,
safe mode, update snapshot and rollback.

The exact rebuilt bundle launched through CUA. The UI driver still cannot bind
the tray-only interface, so live menu-bar pixels were not captured; the source
mask was visually inspected and the new raw bitmap was verified embedded in
the running shell binary. Bundle ICNS bytes match the current export.

At 14:49:58 UTC, one native shell PID 39432 (started 09:49:26 local) and its
child PID 39519 (09:49:27) were running on `127.0.0.1:59160`; health returned
HTTP 200 with all dependencies healthy. Both `/assets/images/brand-icon.svg`
and `/favicon.ico` returned HTTP 200, correct MIME types and exact source bytes.
Ports 4114/4115 are closed. Native data remains one project, one board, zero
tasks/runs/provider accounts; integrity and foreign keys pass.

This is an unsigned local developer build. No remote push, Pages publication,
signed clean-Mac release or real-provider acceptance is claimed. Task 1038's
provider-sign-in handoff remains separate from this completed icon task.

## Refinement — letter C with a sperm tail

The user clarified that the letter C should be the recognizable main form,
with a sperm tail attached. Reopened the same task: replace the oval-headed
curled cell with a round, open C and a separate tapering wave from its lower
curve. Keep the pearl tile, shared violet gradient, exporter and all consumers.
Expand the tray crop to include the tail. Acceptance is matching regenerated
exports, small-size/alpha checks and a rebuilt running app. Ponytail full and
the same quality gates remain in effect; no new dependency or behavior.

The revised color icon and 64 px monochrome silhouette were visually inspected.
`rtk ruby github.page/verify.rb`,
`rtk node test/github_page_art_mode_test.cjs`, and `rtk git diff --check` passed.
A repeated system-Ruby export produced ten identical output hashes. The tray
check passed its 16,384-byte size, black RGB/variable alpha, visible pixels and
transparent outer-edge requirements, confirming the longer tail is not clipped.
The existing Ruby PATH warning remains unrelated to the successful export.

The refined source was committed/merged locally as `33f5c66`.
`rtk env -u CR_PAT ./bin/dev.build` passed again: production assets/release,
6 metadata tests/15 assertions, 10 native tests, Rustfmt/Clippy, app packaging
and all desktop verifier checks. The smallest 16 px favicon was also inspected
at enlarged pixel scale; its open C and trailing wave remain distinguishable.

After identity-checked graceful shutdown of the idle prior app, CUA launched
the revised bundle. The same tray-only binding limitation remains. At 15:03:29
UTC, one shell PID 4664 and its child PID 4751 (started 10:03:03/04 local)
served healthy HTTP 200 at `127.0.0.1:59994`. Served SVG/ICO bytes, bundled ICNS
and the tray mask embedded in the running binary all match the revision.
Native counts remain one project/board and zero tasks/runs/provider accounts;
integrity and foreign keys pass. No remote publication or provider acceptance
is implied by this icon refinement.
