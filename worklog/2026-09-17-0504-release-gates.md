# Worklog — 0504 quality gates, approval, and release handoff

## Metadata

- Date/time (UTC): 2026-09-17
- Task: 0504
- Status: complete
- Human/agent owner: codex
- Branch: `feature/0504-release-gates`
- Start revision: `264fee3`
- End revision: pending commit

## Acceptance criteria

- Validate exit-gate artifacts with typed schemas and return structured QA findings.
- Show the candidate diff, evidence, tests, and knowledge citations at the human approval boundary.
- Refuse protected-branch or unapproved pushes.
- Keep provider/VCS credentials inside host services and outside every agent process.
- Create a draft PR whose body includes evidence and knowledge citations through a replaceable `VcsHost` behaviour.
- Prove approval-to-push/draft-PR handoff with local bare-remote and fixture-host tests.

## Reference coding

- Focused XERJ searches of the project and pinned Vibe, Agetor, and Hydra indices were attempted before implementation. The configured XERJ service at `127.0.0.1:9200` was unreachable, so retrieval remained explicitly degraded.
- Inspected pinned Agetor revision `eb74ab5f3d6d71dfb91d5bd34e9c679c5a955a6a` under its MIT license. `src/bun/github.ts:60-120` constrains the GitHub API origin, while `src/bun/github.ts:1610-1650` validates the PR inputs, requires a token, and sends a draft-PR payload.
- Adapted the boundary, not source code: Cuckoding fixes production API calls to `https://api.github.com`, keeps the credential in `SecretStore` and host-only process/request data, validates its own typed evidence, and uses replaceable fixture callbacks for tests. No upstream code was copied.

## Work performed

- Claimed task 0504 and restated its acceptance criteria.
- Added a versioned typed gate evaluator with structured QA findings, required evidence types, safe relative paths, and SHA-256 integrity checks for artifacts and project-knowledge citations.
- Revalidated evidence immediately before approval and exposed candidate base/head, changed files, tests, artifacts, and knowledge citations in the keyboard-accessible approval UI.
- Added a shared release policy that rejects missing/wrong approval, run mismatches, and `main`, `master`, or the configured default branch.
- Added `GitHubVcsHost`: the trusted snapshot holds only an opaque credential reference; the host fetches and audits the secret, performs a non-force push, and posts a draft PR to the fixed GitHub API origin. Raw credentials are absent from agent requests, commands, events, artifacts, remote URLs, and PR content.
- Kept the existing local bare-remote handoff and made the walking skeleton select a `VcsHost` without changing the default demo path.
- Updated architecture, flow, security, and walking-skeleton documentation to match shipped behavior.

## Verification

- `rtk mix test test/cuckoding/workflows/gate_evaluator_test.exs test/cuckoding/walking_skeleton_test.exs` — passed: 11 tests, 0 failures. Covers structured failed-QA findings, malformed evidence, artifact tampering, approval review UI, unapproved/protected branch refusal, local bare push, credential access audit, and draft GitHub PR payload.
- `rtk mix compile --warnings-as-errors` — passed.
- `rtk mix quality` — passed: formatter check; unused dependency check; compiler with warnings as errors; 10 properties and 114 tests with 0 failures; Credo strict over 98 source files and 1,338 modules/functions with no issues; Sobelow clean; Hex audit found no retired or advisory packages.
- `rtk git diff --check` — passed before commit.

## Handoff

Complete. GitHub network behavior was exercised through a deterministic fixture rather than a live repository, so no external branch or pull request was created. The real local-bare Git push path passed end to end.
