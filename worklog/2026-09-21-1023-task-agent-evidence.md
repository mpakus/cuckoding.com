# 1023 — Task-linked agent evidence and Development startup

Claimed on `fix/1023-task-agent-evidence` from clean `main`. Acceptance is the checklist in the task file. Ponytail 4.10.0 (MIT, full mode), workflow-and-kanban, Elixir/Phoenix LiveView, security-review, and quality-gates apply.

Initial read-only diagnosis: run `01a0bcd7-0e7d-7df7-8263-a71361f5de57` has a successful Specification attempt, then Development failed within 9 ms with no implementer session or process. Its redacted Codex JSONL artifact contains a substantial agent-written specification and shell commands that did not use RTK. The `workflow.failed` event recorded only `unexpected_failure`. Source inspection found that saved Cursor launches supply the fixed non-secret `AGENT_CLI_CREDENTIAL_STORE=file`, while `LocalProcessRunner` rejects every environment key containing `CREDENTIAL`. The resulting tuple-valued adapter error is passed to `OrchestrationFailure.failure_code/2`, which calls `to_string/1` on the tuple and raises, masking the original error as `unexpected_failure`. This is an inference from the deterministic code path; no historical exception stack was preserved. No user run was retried or changed.

Reference coding: `rtk xerj search` could not reach the local XERJ node on port 9200, so I inspected project source directly. The pinned Apache-2.0 Vibe Kanban peer (`735654971bd396aa97b65166955678e4c34f8bf8`, `crates/db/src/models/coding_agent_turn.rs:1-44`) keeps agent-turn evidence linked to a process. No code was copied. Cuckoding instead reuses its own redacted, task-correlated `RunEvent` stream and idempotent event commands.

Change: allow only the non-secret Cursor selector `AGENT_CLI_CREDENTIAL_STORE=file` through the shared process runner; preserve safe tuple error codes through workflow and provider failure recorders. Include an approved installed RTK path in all stage/planning objectives, with an explicit unavailable fallback. This is agent guidance only, not command-level enforcement over provider-native tools. Persist public agent messages from the redacted JSONL artifact at process completion, before handling exit status. Save the actual structured or final public Specifications text immediately as a versioned artifact. Show the latest specification and bounded task timeline in LiveView, refreshing from committed events. Tool output and hidden reasoning are not copied to task messages; the run's filtered log remains the full diagnostic source.

Historical recovery: scoped idempotent indexing copied the three public Codex messages from the exact redacted artifact of this run into its durable event stream. The first maintenance attempt committed one message but failed broadcasting because the isolated `mix run --no-start` process lacked PubSub; rerunning with a temporary local PubSub supervisor completed the other two without duplication. A read-only SQLite count returned exactly three `activity.summary` events at sequences 17–19. No task state, run state, worktree, or agent process was changed. The in-app browser visibly showed all three messages and the full specification on the task page.

Verification:

- `rtk env -u CR_PAT mix format` — passed.
- `rtk env -u CR_PAT mix test test/cuckoding/adapters/output_parser_test.exs test/cuckoding/walking_skeleton_test.exs test/cuckoding/board_task_intake_test.exs test/cuckoding/project_workflow_test.exs test/cuckoding/execution/local_process_runner_test.exs` — 46 tests, 0 failures; an initial compiler warning about noncontiguous clauses was fixed.
- `rtk env -u CR_PAT mix test test/cuckoding/walking_skeleton_test.exs` — 23 tests, 0 failures after the plain-text specification fallback.
- `rtk env -u CR_PAT mix credo --strict` — no issues after refactoring nested clauses.
- `rtk env -u CR_PAT mix quality` — 279 tests and 10 property checks, 0 failures; Credo clean; Sobelow and dependency advisory scans clean. The suite emitted expected fixture process crashes and transient SQLite busy/retry warnings; neither failed a test.
- After adding stage transitions to the task timeline, `rtk env -u CR_PAT mix test test/cuckoding/project_workflow_test.exs` passed 6 tests and a final `rtk env -u CR_PAT mix quality` again passed 279 tests, 10 property checks, strict Credo, Sobelow, and dependency advisories. `rtk git diff --check` was clean.
- Browser inspection at `/boards/01a0ba83-3456-7ceb-a824-e59902f563b1/tasks/01a0bb7c-f6d3-776c-bffe-7da4600ebe81` — specification and three preserved messages visible, run still blocked.

`rtk proxy` exceptions were used for exact unfiltered source excerpts, Git diff, read-only SQLite evidence, and OS/process inspection; filtering there could hide lines or alter SQL/text output. Product commands remain policy-validated before optional RTK wrapping. Remaining limits: provider-native shell calls are not forcibly RTK-wrapped; public messages become task events after each provider process completes, while stage transitions and run logs remain live; task timeline displays at most the latest 5,000 entries and links to complete filtered logs.
