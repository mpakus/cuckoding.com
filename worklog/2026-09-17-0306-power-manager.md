# Worklog — 0306 Power assertions and sleep/wake reconciliation

## Acceptance criteria

- Hold an idle-sleep assertion only for eligible active work or an active unattended window.
- Detect and durably record continuous-minus-uptime sleep gaps, extend eligible leases before expiry, and reconcile each affected run without duplicating its stage attempt.
- Keep pending approvals pending in unattended mode.
- Pass the simulated gap suite and preserve the committed opt-in real-sleep evidence from task 0004.

## Reference coding

- Project XERJ search: `CLOCK_MONOTONIC CLOCK_UPTIME_RAW caffeinate sleep gap power assertion reconcile`.
- The measured implementation at `spikes/0004-sleep-wake-power/power_manager_spike.rb:13-72,74-109` supplies the clock-divergence, supervised assertion, and gap-idempotency behavior already accepted by ADR-022.
- Peer XERJ search: Hydra `resume persisted session sleep wake process lifecycle`; it confirmed persisted-session recovery as a boundary but contained no power-specific implementation to adapt.

## Verification

| Check | Result |
| --- | --- |
| `rtk mix test test/cuckoding/power/manager_test.exs` | Pass: 4 tests, 0 failures, including the real `caffeinate` hold/release lifecycle and stable-cycle wake retry. |
| `rtk mix quality` | Initial functional suite passed (8 properties, 70 tests); strict Credo found one small nesting issue, simplified before the final run. |
| `rtk mix quality` (final) | Pass: 8 properties, 71 tests, 0 failures; warnings-as-errors compile, strict Credo, Sobelow, and dependency audit passed. |
| `rtk git diff --check` | Pass. |
| XERJ generation 22 dry-run / refresh | Dry-run passed. The actual refresh aborted with HTTP 429 after the disk reached 98% and XERJ reapplied its read-only flood-stage block. Generation 21 remains the last committed manifest; the temporary 99% override was restored to `null` and verified. |

## Work performed

- Added checked `power_events` storage for assertion, sleep-gap, wake, and unattended-window events.
- Added a supervised power manager with injectable samples, assertion driver, and reconciler; production uses exact macOS continuous/uptime clocks and a sterile `caffeinate` child.
- Delegated every detected gap to the existing lease-first startup reconciler under a stable power-event cycle ID, preserving its process, session, port, dev-server, worktree, and command recovery classifications.
- Added durable unattended-window changes without any approval mutation or bypass.
- Added simulated-gap, tolerance, assertion eligibility, pending-approval, unattended-window, native clock, and real assertion-process regression coverage.
- Retained failed wake reconciliation under its original power-event cycle ID and retry it on later ticks without duplicate gap events.
- Reused the committed task 0004 opt-in software-sleep and AC/battery lid-close evidence; this turn did not force the user's machine to sleep.
