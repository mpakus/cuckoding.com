# 1045 — Readable run activity and failures

Acceptance: last 30 events with automatic/accessibly controlled history, bounded
LiveView state, stable reading during updates, consistent Plugins panel, and
human-readable shell activity plus a durable failure explanation.

Started from clean main. Branch `feature/1045-run-activity`. Applied Ponytail
4.10.0 full (MIT), LiveView, observability, accessibility, layout, writing,
security review and quality gates. No runtime permissions or workflow decisions
change. No data migration or historical-event rewrite.

Evidence: supplied native run has 289 events. AgentFloor loaded the first 200,
so workflow.failed at sequence 288 never reached the failure banner. The saved
failure code is generic workflow_failed; no precise historical cause is known.
The RTK importer retained only a generic label and discarded command outcome.

XERJ port 9200 had no listener. Used source directly: installed LiveView
stream API and official 1.2.12 scroll-event guide; pinned vibe-kanban
`packages/ui/src/components/ChangesPanel.tsx:110-125` (Apache-2.0) demonstrates
bounded visible history. Reuse native LiveView streams/viewport events instead
of adding that peer's React virtualizer. SQLite event sequence owns cursors;
browser scroll state never controls workflow state.

`rtk proxy` exceptions: exact source/protocol/SQLite reads, scripted edits,
test/build diagnostics. The in-app browser rejected the supplied loopback URL
with ERR_BLOCKED_BY_CLIENT; native/browser verification remains pending.

Implemented a latest-30 projection with exclusive sequence cursors and native
LiveView viewport events. Browsing keeps at most 90 mounted entries and preserves
loaded history during refresh. Failure lookup is independent of pagination.
Plugins now share the other panels' border/background/spacing. Historical RTK
entries are rendered with explicit request/unknown-outcome wording; newly
imported tool outcomes retain only typed exit status/error flags. Raw command
strings/output are not duplicated. The latest log is selected on page entry.

Verification:
- `rtk env -u CR_PAT mix test test/cuckoding_web/agent_floor_live_test.exs test/cuckoding/adapters/output_parser_test.exs test/cuckoding/activity_stream_test.exs`:
  25 tests, 0 failures. Long-run regression covers 301 events without gaps,
  a failure outside the visible window, older/newer paging, stream bounds,
  live refresh while browsing, reconnect, ignored client cursors, and return to
  latest. Adapter tests cover typed exits and command/output secret canaries.
- `rtk env -u CR_PAT mix quality`: formatter, unused dependencies, warnings-as-errors
  compiler and full suite passed (10 properties, 351 tests, 0 failures). Credo
  caught alias ordering; corrected it.
- `rtk env -u CR_PAT mix format` and `rtk env -u CR_PAT mix test test/cuckoding_web/agent_floor_live_test.exs`:
  passed after the ordering fix and added boolean-error regression (12 tests).
- `rtk env -u CR_PAT mix credo --strict`, `rtk env -u CR_PAT mix sobelow --config`,
  `rtk env -u CR_PAT mix hex.audit`: passed, no findings/advisories.
- `rtk env -u CR_PAT ./bin/dev.build`: production compiler/assets/release passed;
  Ruby metadata 6/15 and restart 6/98 passed; Rust 10 tests, fmt and clippy passed;
  native bundle and sterile authentication, shutdown, safe-mode, update/rollback
  verifier passed. Unsigned local developer build, not a signed release.
- Read-only native SQLite inspection: integrity ok, blocked 1 / done 1 runs,
  all 6 process records exited before restart. No migration or data rewrite.
- `rtk ./bin/dev.restart`: graceful shutdown of owned PID 21607, cleanup and
  healthy rebuilt app at `http://127.0.0.1:64985`. Ruby reported an existing
  world-writable DBngin PATH directory warning; restart succeeded.

Visual verification is pending browser authentication. The isolated browser
reports ERR_BLOCKED_BY_CLIENT for loopback; native Chrome reached the rebuilt
app's authentication screen. The menu-bar app exposes no accessible UI to the
automation tool; asked the user to open its normal authenticated dashboard.

Security review: new browser inputs trigger read-only run-scoped sequence
queries; cursor values come from server assigns, not browser payloads. No new
execution, filesystem grants, authentication bypass, dependencies, database
tables, or historical rewrites. Existing redactor and log confinement tests
pass. `rtk env -u CR_PAT mix format --check-formatted` and `rtk git diff --check`
pass. Native data and the original blocked run are retained.
