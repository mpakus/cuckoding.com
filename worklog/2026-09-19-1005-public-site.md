# Worklog — 1005 Sync the Public Site with the Current Product

## Metadata

- Date/time (UTC): 2026-09-19T14:18:30Z
- Task: 1005
- Status: done
- Human/agent owner: Codex
- Branch: `feature/1005-public-site-motion`
- Start revision: `53d288baf6cd0c81b8c6e1c8519a43542d478f5b`
- End revision: `53d288baf6cd0c81b8c6e1c8519a43542d478f5b` plus the uncommitted task patch
- Environment and relevant tool versions: dependency-free static GitHub Pages
  source; native CSS, IntersectionObserver, requestAnimationFrame; Ponytail
  4.10.0, MIT license, full mode

## Intended outcome

Synchronize the public site with the landed project → board → task → prepared
run and monitoring surfaces, preserve honest controlled-beta limitations, and
add restrained accessible motion and decorative parallax.

Acceptance requires truthful current/pending labels, native dependency-free
motion, reduced-motion parity, semantic keyboard-accessible markup, 320 px
reflow, and focused source plus browser verification.

## Context inspected

- Clean `main` worktree at the start revision.
- `AGENTS.md`, mandatory product/architecture/database/flow/security/execution
  documents, `README.md`, `CHANGES.md`, `docs/BETA_REPORT.md`, and the current
  GitHub Pages source and verifier.
- Latest workflow commit and its project, board, task, prepared-run, dashboard,
  and Agent Floor documentation.
- Ponytail 4.10.0 (MIT), repository Ponytail and quality-gates skills, and the
  animation/accessibility guidance.

## Work performed

- Replaced stale "next in beta" board and monitoring claims with the implemented
  project → board → Draft/Ready task → prepared run → run-scoped authentication
  flow.
- Added a dated current-situation section that separates landed source features
  from fresh signed-build, telemetry, dogfood, sleep/relaunch, and interview
  evidence still required before release.
- Reframed the feature matrix around the current setup, workflow, operations,
  recovery, knowledge, plugin, and native-shell surfaces.
- Added eleven decorative parallax targets, one-shot clip and translate reveals,
  and short staggered status/workflow transitions using the existing native
  script and approved easing tokens. Meaningful primary artwork and readable
  content remain stable.
- Kept touch hover gating, 44 px targets, skip navigation, semantic headings,
  intrinsic image sizes, reduced-motion behavior, and 320 px reflow.
- Updated the focused verifier to reject the stale labels and require the
  current workflow, release boundary, layered parallax, motion tokens, and
  reduced-motion cleanup.
- Converted the four wide concept scenes and the square composition into
  full-section backgrounds, distributed all six person assets as decorative
  parallax foregrounds, and removed the redundant static crew gallery.
- Preserved the existing finale as the composition reference and updated the
  footer to the exact linked Austin/Texas wording requested.
- Moved the hero artwork caption above the content panel at the 320 px
  breakpoint after the visual pass found an overlap.
- Reopened for a focused art-direction follow-up: replaced the repeated green
  robot in the truth scene with `person1.webp` while leaving the finale intact.

## Artifacts

- Commits/patches: uncommitted working-tree patch on
  `feature/1005-public-site-motion`
- Migrations: none
- Logs/reports/screenshots: local browser inspection at default 1280×720 and
  exact 320×844 viewports
- Configuration or policy hashes: none

## Verification

| Command or check | Result | Evidence/notes |
| --- | --- | --- |
| `rtk mix phx.routes` | pass | Current project, board, task, run, Agent Floor, knowledge, plugin, health, and shell surfaces confirmed; rerun outside the filesystem sandbox because Mix PubSub needs a local socket. |
| `rtk node --check github.page/script.js` | pass | No JavaScript syntax errors. |
| `rtk ruby github.page/verify.rb` | pass | 14 local references and 4 pinned Pages actions verified, along with current-state copy, image, motion, and safety assertions. |
| `rtk git diff --check` | pass | No whitespace errors. |
| `rtk python3 -m http.server 8765 --bind 127.0.0.1 --directory github.page` | pass | Updated HTML, CSS, JavaScript, and all supplied site assets returned HTTP 200/304 while inspected. |
| Desktop browser at 1280×720 | pass | `scrollWidth` matched 1280 px; scene backgrounds, glass copy panels, parallax foregrounds, unchanged finale, footer link, and captions rendered cleanly; the console had no warnings/errors. |
| Mobile browser at 320×844 | pass | `scrollWidth` matched 320 px; backgrounds, foreground people, footer wrapping, and the unchanged finale reflowed cleanly after the hero-caption overlap was corrected. |
| Targeted truth/finale browser pass at 1280×720 | pass | The truth scene now shows the planning robot while the adjacent finale retains its original green and orange robots. |
| Runtime reduced-motion emulation | skipped | Browser viewport controls do not expose media emulation; the verifier checks the CSS opt-out and JavaScript parallax reset, and the reduced-motion source path was inspected. |
| Full application quality gate | skipped | No Phoenix, Elixir, Rust, migration, configuration, or application asset code changed; focused static-site gates are proportionate. |

## Telemetry and operational evidence

No product runtime or external provider is involved in this static-site task.

## Decisions and deviations

- Created a focused 1005 task instead of claiming the broader unfinished 1004
  release-readiness task.
- `rtk proxy cat` and `rtk proxy sed` were used for exact, unfiltered reading of
  binding instructions, documentation, source, and prior evidence.
- No new dependency, framework, animation library, or generated artwork.
- The first sandboxed server attempt could not bind a socket. The first approved
  retry used Python's default `::` bind and was stopped immediately before a
  browser request; the actual verification server was rebound explicitly to
  `127.0.0.1`.

## Risks and blockers

- Deployment is intentionally outside this task; GitHub Pages publishes only
  after a later merge to `main`.
- Reduced-motion behavior has structural evidence but was not visually emulated
  in the browser tool used for this task.

## Handoff

The focused site update is complete in the working tree. It is not deployed;
GitHub Pages will publish only after a later merge to `main` and a successful
Pages workflow.

## Checklist

- [x] Task acceptance criteria reviewed.
- [x] Relevant documentation updated.
- [x] Tests and checks recorded honestly.
- [x] Secrets and sensitive content excluded/redacted.
- [x] Residual risks and skipped work are explicit.
- [x] Task status and next owner are updated.
