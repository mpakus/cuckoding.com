# 1017 — Human-friendly UI

## Acceptance and baseline

Review the complete project → agents/roles → board → task → run journey, reduce unnecessary scrolling and jargon, clarify saved versus unsaved changes and next actions, preserve trust gates, and verify responsive/keyboard behavior with focused regressions. Only one task claimed: 1017.

Preserved task 1016 in its own local commit before branching. Unrelated `icon.png` is untouched. No existing user task, project, credential, or run will be changed during browser verification.

## Skills and references

- Ponytail 4.10.0 full, MIT; repo LiveView, workflow, quality-gates, and security-review.
- better-ui, better-layout (spacing/adaptivity), better-accessibility (forms), better-writing: native controls, progressive disclosure, clear next actions and contextual feedback, not a new design framework.
- Required core docs and current source read before changes. Memory registry lookup found no applicable in-app implementation guidance; no memory fact used.
- `rtk xerj search --prefix cuckoding-project-v7 -k 3 'LiveView board task filters native details form unsaved changes'`: local node unavailable; direct-source fallback.
- Agetor MIT, pinned `eb74ab5f3d6d71dfb91d5bd34e9c679c5a955a6a`, `src/mainview/components/kanban/ReferencesPicker.tsx:235-270`: native details/summary for optional form sections. Adapt the disclosure idea using existing Phoenix components; no React or source copied. Cuckoding keeps durable workflow state and confirmation gates.
- RTK proxy is used for exact instruction/source reads and script verification where filtering would hide required content. Mix may need escalation for its local IPC socket.

## Findings and changes

- Shared Projects / Agent activity / Knowledge navigation and section shortcuts for dashboard, project settings, and runs. Dashboard creation age no longer claims execution elapsed time.
- Three-step onboarding keeps edits when going Back, announces step changes, explains branch/setup effects, and offers cancel/loading feedback.
- Project settings explain connections versus roles versus recorded sessions; saved-agent sign-in is in a native disclosure. Visible dirty state and a server-side LiveView guard prevent creating a board from unsaved defaults. Removing a project connection requires confirmation; no credential or runner changes.
- Board create/planning forms use native keyboard-operable disclosures. Default columns are Draft/Ready/Running/Waiting/Done plus occupied uncommon states; all states remain accessible through Show all states and native move controls. Filters have a reset, clear empty states, and a state-specific view. Committed activity events coalesce into database reloads.
- Task next actions are state-specific; unsaved edits block Mark Ready and Prepare run until saved. Locked tasks retain readable descriptions. Removed the unmeasured zero-knowledge card placeholder.
- Completed planning proposals collapse for easier log access; remaining proposals retain their existing import action. Agent role labels use the existing human-readable helper. Activity/process tables have keyboard-scrollable regions; narrow headings no longer wrap letter by letter.
- No dependency, migration, policy change, auto-launch, authentication redesign, or user-data mutation. Existing board assignment snapshots remain immutable.

## Verification

- `rtk env -u CR_PAT mix test test/cuckoding_web` — 43 tests, 0 failures (initial focused pass).
- `rtk env -u CR_PAT mix quality` — final expanded source/behavior checks passed: 249 tests + 10 properties, 0 failures; formatting, unused-dependency check, warnings-as-errors compile, strict Credo, Sobelow, and Hex audit passed. Expected crash-supervisor fixture logs are not failures.
- `rtk env -u CR_PAT mix compile --warnings-as-errors` — passed separately.
- `rtk env -u CR_PAT mix assets.build` — Tailwind and esbuild passed.
- `rtk proxy node test/js/log_tail_test.cjs` — passed retained log-follow regression; proxy preserves exact test output.
- `rtk git diff --check` — passed.
- Browser: inspected dashboard, onboarding steps 1/2, project saved agents and roles, board filtering/disclosures, Ready task, completed run, and agent activity/inspector. Verified wizard branch edits survive Back/Continue; did not finish registration or choose a user folder. Review/registration and mutation paths are covered by LiveView fixtures, not a new real-project run.
- At 320px, inspected the journey and measured document width equal to viewport for dashboard, board, task, project settings, wizard repository step, agent floor/inspector, and run (no page overflow). Desktop dashboard inspected at 1280px. Native summary Enter, filtered Ready task navigation, and keyboard horizontal activity-table scrolling verified. One table DOM geometry query timed out; screenshot and keyboard verification succeeded.
- Browser console continues to report an unattributed MutationObserver `observe` error already recorded in task 1016. No matching application-owned observer implementation was identified; UI interactions worked. This is not a clean-console claim.
- Limits: unsaved indicators are not persistent draft storage or a browser-leave guard. No screen-reader software, physical-device, packaging, real-provider authentication/launch, sleep/recovery, or release validation was run for this UI-only change. User projects, boards, tasks, credentials, and runs were not changed during browser checks. XERJ unavailable as noted above.
