# Worklog — 1016 live run logs

## Scope and acceptance

Implement a bounded LiveView tail of recorded process artifacts, latest 5,000 lines plus a full filtered download, and make Ready-task setup/start navigation obvious. No automatic provider launch, workflow redesign, credential changes, or mutation of the user's example task/run.

## Baseline and references

- Branch `feature/1016-live-run-logs` starts from docs commit `35678fe`; preserved untracked `icon.png`.
- Read core product/architecture/database/flow/security/execution docs and applicable skills: Ponytail 4.10.0 full (MIT), LiveView, workflow/kanban, observability, security-review, writing, quality-gates.
- `rtk xerj search --prefix cuckoding-project-v7 -k 3 'artifact log tail redaction'` — node unavailable. Used direct source fallback.
- Pinned Agetor MIT `eb74ab5f3d6d71dfb91d5bd34e9c679c5a955a6a`, `src/bun/claude-tmux.ts:3180-3203`: bounded positional tail, dropping a partial leading line. Adapt the approach, not source; Cuckoding additionally resolves recorded run ownership, rejects symlinks, normalizes provider events, and authenticates downloads.
- Reuse provider `decode_event/2` and recursive redaction; never expose raw provider JSON or hidden reasoning. Existing runner output remains on disk, unchanged.
- RTK proxy exceptions: exact full-file reads and read-only source/test scripts, because filtered output can hide required instructions or alter evidence.

## Verification

No live user task is started by this work.

- Initial focused/full checks caught the macOS relative `/var -> private/var` alias and an incorrect test request host. Fixed both without relaxing arbitrary symlink rejection. Credo findings were resolved using a case and a small chunk callback.
- `rtk env -u CR_PAT mix format` — passed; Mix required escalation for its local IPC socket after sandbox denial.
- `rtk env -u CR_PAT mix compile --warnings-as-errors` — passed before final verification.
- `rtk env -u CR_PAT mix test test/cuckoding_web/agent_floor_live_test.exs test/cuckoding_web/board_live_test.exs` — 11 tests passed, before adding two further full-suite regressions.
- `rtk proxy node test/js/log_tail_test.cjs` — passed: initial scroll, appended-output follow, preserve scrolled-up position, changed-log reset. Proxy preserves exact script output.
- `rtk env -u CR_PAT mix assets.build` — passed, Tailwind and esbuild.
- `rtk env -u CR_PAT mix quality` — passed, 246 tests + 10 properties, Credo no issues, Sobelow scan complete, no retired/security advisory packages. Expected supervised fixture-crash messages are test evidence, not failures. Includes authenticated download success, unauthenticated/non-loopback denial, hardlink/symlink rejection, oversized/invalid bytes, file replacement, and prepared-run navigation.
- `rtk git diff --check` — passed.
- After the final wrapping-style change: `rtk env -u CR_PAT mix format --check-formatted`, `rtk env -u CR_PAT mix assets.build`, and the focused two-file test command all passed (13 tests). Final browser screenshot confirmed wrapped output in the bounded log panel. Browser geometry evaluation timed out after reload; visual verification succeeded instead.

## Browser evidence and boundaries

- Verified the supplied run on loopback: Done, imported proposals, actual log artifact renders 10 public lines, pause/resume updates the status. The artifact UUID differs from its process-record UUID; resolve the artifact within its recorded run instead of guessing that association.
- Verified Echo's Ready card → Set up and start → task detail with Prepare run, without triggering preparation or a provider. Existing queued-task navigation is covered by regression tests.
- At the default 1280px browser viewport the document remained 1280px wide; log scrolling stayed inside its panel. Long public JSON rows wrap for readability. Browser console reports a MutationObserver non-Node error without a source URL; no matching observer call exists in app JavaScript/LiveView source. Origin remains unverified; log controls and navigation worked. Do not claim a clean browser console.
- Ponytail kept the change to a read-only file projection, existing provider decoders/redactor, and native controls; no new dependency or execution state machine.
- Preview: 5,000 lines / 8 MiB; 64 KiB per supported provider row. Full download streams the same filtered public representation. Unknown/unstructured rows are omitted rather than accidentally serving private reasoning or credentials. Downloads use loopback and session checks; no raw-file endpoint.
- No packaging/clean-machine or provider execution smoke test: this change does not alter runner/packaging, and launching the user's task is outside this verification scope.
