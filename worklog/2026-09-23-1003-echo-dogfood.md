# Worklog — 1003 local Echo dogfood follow-up

## Claim and intended outcome

- Task 1003 remains in progress; this entry covers the local Echo board and a
  shared Review-schema fix under the existing 0405 walking-skeleton boundary.
- Merge the tested board-agent assignment and stage-budget fix to local main,
  diagnose current blocked runs, preserve historical evidence, and make the
  operator documentation reflect current source behavior.
- This is not controlled-beta or signed-build acceptance. Do not launch another
  paid provider run until the previously exposed key's rotation is confirmed.

## Evidence and changes

- Fast-forwarded `feature/1031-board-agent-auto-run`, then merged
  `fix/0405-stage-budget-timeout` into local `main` at `3c8f634`. The live app
  was restarted from the merged checkout. An unrelated modernization branch
  that removes extensive Cuckoding documentation was excluded from this merge.
- Echo skeleton run `01a0cc65-6e30-7674-b71a-9d91424debbe` hit
  `process.timeout` at five minutes, then exit 130. The former request limit
  was 300,000 ms while the saved Coding-stage budget was 3,600,000 ms. The
  prior 0405 fix corrects the request and its focused regression.
- Echo source-validation run `01a0cc63-df9a-774e-92cc-4137f78f498e` and
  source-decision run `01a0cc65-382e-75af-95e6-63bc9bf35f4b` each reached
  Review, then their redacted process logs reported `invalid_json_schema` for
  `findings[].evidence`: `additionalProperties` must be false. The shared
  Review schema now declares a required `detail` string and closes that object.
  The existing persistence path still stores evidence as a map. OpenAI's
  structured-output documentation confirms the closed-object/required-field
  contract; no new dependency or adapter branch was added.
- Echo's own `docs/TASKS.md` still has every Phase 0 source-validation item
  unchecked and explicitly forbids Phase 1 until a real permitted source and
  scrubbed fixtures exist. No board task dependency edges encode that phase
  gate. The two Phase 0 run branches contain clean commits documenting NO-GO,
  absent live responses, and absent fixtures; they are not merged into Echo
  main. The Phase 1 skeleton's partial generated files remain in its old run
  worktree; no automatic retry or cleanup occurred.
- Updated the documentation audit, release operator flow, and beta finding
  ledger to distinguish source fixes from real-provider retest and the Echo
  project gate.

## Verification

| Check | Result |
| --- | --- |
| `rtk mix quality` on merged main before Review-schema edit | pass: 10 properties, 302 tests, 0 failures; strict Credo, Sobelow, dependency audit clean |
| `rtk mix test test/cuckoding/walking_skeleton_test.exs:309` | pass: 1 focused regression, 0 failures |
| `rtk mix quality` after Review-schema edit | pass: 10 properties, 302 tests, 0 failures; strict Credo, Sobelow, dependency audit clean |
| `rtk curl -sS -o /dev/null -w '%{http_code}' http://127.0.0.1:4000/` | 200 after merged-main restart, before Review-schema edit |
| `rtk proxy git diff --check` | pass after removing a Markdown trailing-space hard break; `rtk git diff --check` hid the diagnostic while retaining exit 2, so the proxy preserved exact Git output |
| Restarted `rtk proxy env PHX_SERVER=1 mix phx.server`, then fetched the Echo task route | pass: loopback listener on port 4000 and HTTP 200; browser still correctly shows historical run 2 as blocked |
| Live SQLite run counts after restart | unchanged: 5 blocked, 2 done, 4 failed, 2 queued; no project autopilot active |

## Handoff

- Confirm provider-key rotation before another paid provider run. Confirm Echo
  Phase 0 evidence and source choice before allowing Phase 1 admission.
- Historical blocked runs are immutable. Review the preserved partial worktree
  before any new run; the normal retry does not copy its uncommitted files.
